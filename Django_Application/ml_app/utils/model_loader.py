"""
Backward-compatible import path.

Canonical implementation lives in `ml_app/model_loader.py` (per project spec).
"""

from ml_app.model_loader import InferenceResult, get_model, run_inference  # noqa: F401

